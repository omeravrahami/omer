// AdMob Banner Placeholder
// TODO: Replace with react-native-google-mobile-ads BannerAd when building native app
// This component renders an empty View that will be replaced with real ads post-publish
import React from 'react';
import { View } from 'react-native';

export const AdBanner = () => {
  // AdMob requires native build - placeholder for now
  return <View style={{ height: 0 }} />;
};
